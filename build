export GO_ENV=prod
export RELEASE=/tmp/game

./deps
train
bee pack
mkdir -p $RELEASE
mv language-game.tar.gz $RELEASE

cd $RELEASE
tar -xzf language-game.tar.gz
killall language-game
nohup ./language-game &

unset GO_ENV
unset RELEASE
