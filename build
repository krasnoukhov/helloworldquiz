export GO_ENV=prod
export RELEASE=/tmp/game

./deps
train
bee pack
mkdir -p $RELEASE
mv langgame.tar.gz $RELEASE

cd $RELEASE
tar -xzf langgame.tar.gz
killall langgame
nohup ./langgame &

unset GO_ENV
unset RELEASE
